# AAC-0029 — Every claim is supported by retrieved context
# Normative. Edit here; the rendered site is generated from this file.
id: AAC-0029
legacy_id: A3-02
status: active
since: "0.1.0"

level: MUST
dimension: grounding
gate: true

# Applies to these archetypes. CORE cases list every archetype explicitly
# rather than relying on an inheritance keyword, so a consumer never has to
# resolve the taxonomy to know whether a case applies.
archetypes:
  - A3
  - A6
core: false

title: |-
  Every claim is supported by retrieved context

statement: |-
  Faithfulness scored claim by claim against the context actually supplied. This is the central obligation of the archetype — a fluent unsupported answer is the failure mode users cannot detect.

# Realization is advisory: it names the class of machinery that can discharge
# the obligation, never a product. See docs/REALIZATION.md.
mechanisms:
  - M3
  - M4
stages:
  - S3
  - S5
tool_class: |-
  Metric library

# Crosswalks to external frameworks live in /crosswalks, keyed by this id.
