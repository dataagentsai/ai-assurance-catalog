{
  "name": "ai-assurance-catalog",
  "version": "0.13.0",
  "description": "Test obligations for AI applications, by architecture archetype. A catalog, not a framework.",
  "license": "Apache-2.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "lint": "node tools/lint.js",
    "render": "node tools/render.js",
    "build": "npm run lint && npm run build-example && npm run render && npm run check-page",
    "test": "npm run lint && npm run build-example && npm run validate-report -- examples/coverage-report.example.json",
    "validate-report": "node tools/validate-report.js",
    "build-report": "node tools/build-report.js",
    "build-example": "node tools/build-report.js examples/aac.config.yaml -o coverage-report.example.json",
    "badge": "node tools/badge.js",
    "check-page": "node tools/check-page.js site/index.html"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "fast-xml-parser": "^4.5.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
